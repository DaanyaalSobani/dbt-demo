version: 2

models:
  - name: customer_orders
    description: One row per customer with order rollups.
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
      - name: order_count
        tests:
          - not_null
      - name: lifetime_value
        tests:
          - not_null
