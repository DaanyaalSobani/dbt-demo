# dbt for local development.
# Unit tests (models/**/_unit_tests.yml) were added in dbt-core 1.8, which is
# the only version constraint this project actually depends on.
dbt-core>=1.8
dbt-postgres>=1.8
